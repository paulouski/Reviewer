# Reviewer

AI Interview Trainer - A serverless application for conducting technical interviews using OpenAI's Responses API.

The API key is stored in the browser's local storage and encrypted to protect against XSS attacks.